//렌더링이란 HTML, CSS, 자바스크립트 등 개발자가 작성한 문서가 브라우저에서 출력되는 과정을 말한다.
//브라우저마다 다르지만, 브라우저는 렌더링을 수행하는 렌더링 엔진을 가지고 있다.
//크롬은 블링크, 사파리는 웹킷, 파이어폭스는 게코... 등

//렌더링 과정
//1. DOM(Document Object Model), CSSOM(CSS Object Model) 생성
//사용자가 브라우저를 통해 웹 사이트에 접속하면, 서버로부터 HTML, CSS 등 웹사이트에 필요한 리소스를 다운받는다.
//브라우저가 페이지를 렌더링하려면 먼저 HTML 코드는 DOM 트리, CSS는 CSSOM 트리를 생성해야 한다.

//2. 렌더링 트리 생성
//DOM 트리와 CSSOM 트리가 만들어지면, 이 둘을 결합해서 렌더링 트리를 생성한다.

//3. 레이아웃 단계
//viewport 내에서 각 요소의 정확한 위치와 크기를 정확하게 캡처하는 Box 모델이 출력된다.
//모든 상대적인 측정값은 화면에서 절대적인 픽셀로 변환된다.

//4. 페인팅 단계
//마지막으로 렌더링 트리의 각 노드를 화면의 실제 픽셀로 변환한다.

//리플로우(Reflow), 리페인트(Repaint)
//렌더링 과정이 완료된 후 사용자가 화면에서 다양한 액션을 수행하게 되고, 여기서 발생하는 이벤트로 인해
//새로운 HTML요소가 추가되거나, 기존 요소의 스타일이 바뀌거나 하는 변경이 일어난다.
//이런 변경을 통해 영향을 받게 되는 모든 노드에 대해 렌터링 트리 생성과 레이아웃 과정을 다시 수행하게 된다.
//이런 과정을 리플로우라고 한다.
//이후 리플로우된 결과가 화면에 그려지기 위해 리페인트가 수행된다.

//기존 요소에 변경이 생겼다고 항상 리플로우, 리페인트가 일어나는 것은 아니다.
//레이아웃에 영향이 미치지 않는 단순한 색상 변경 같은 것들은 리페인트만 수행된다.
//리플로우가 일어나는 대표 속성들 - position, width, height, margin, padding, border, border-sidth, font-size, font-weight...
//리페인트가 일어나는 대표 속성들 - background, color, text-doceration, border-style, border-radius...
/**
 * viewport란? chatGpt
 * Viewport는 웹 브라우저에서 웹페이지가 보여지는 영역을 의미합니다.
 * 사용자가 현재 보고 있는 화면의 크기를 나타내며, 이는 디바이스의 크기(예: 데스크탑, 태블릿, 스마트폰 등)에 따라 달라질 수 있습니다.
 * Viewport는 특히 반응형 웹 디자인에서 매우 중요한 개념입니다.
 *
 * 주요 개념
 * Viewport의 크기:
 * 데스크탑 브라우저에서는 일반적으로 브라우저 창의 크기가 viewport의 크기를 결정합니다.
 * 모바일 디바이스에서는 화면 크기, 브라우저의 설정 등이 viewport의 크기에 영향을 미칩니다.
 *
 * 메타 태그를 사용한 viewport 설정:
 * 모바일 기기에서 웹페이지가 어떻게 보일지를 제어하기 위해 <meta> 태그를 사용해 viewport의 속성을 설정할 수 있습니다.
 * 특히 반응형 웹을 만들 때, 모바일에서의 뷰포트 설정은 매우 중요합니다.
 * 기본적인 viewport 설정은 다음과 같습니다:
 * <meta name="viewport" content="width=device-width, initial-scale=1.0">
 *
 * 주요 속성 설명:
 * width=device-width: 사용 중인 디바이스의 화면 너비에 맞춰 viewport의 너비를 설정합니다.
 * 이를 통해 웹페이지가 디바이스 크기에 맞게 표시되도록 할 수 있습니다.
 * initial-scale=1.0: 페이지가 처음 로드될 때의 줌(확대/축소) 비율을 설정합니다. 1.0은 기본 확대 비율로 설정하는 것입니다.
 * minimum-scale, maximum-scale: 사용자가 페이지를 확대하거나 축소할 수 있는 최솟값 및 최댓값을 설정할 수 있습니다.
 *
 * 왜 중요한가?
 * 반응형 웹 디자인에서는 다양한 화면 크기와 해상도에 맞춰 페이지를 디자인해야 합니다.
 * 뷰포트 설정을 통해 모바일 기기에서도 적절한 레이아웃을 유지할 수 있습니다.
 * 기본적으로 모바일 브라우저는 데스크탑 웹페이지처럼 보이도록 기본 줌 아웃 상태로 페이지를 표시하는데,
 * viewport 메타 태그를 설정하면 모바일 화면에 맞는 크기로 페이지가 렌더링되도록 할 수 있습니다.
 *
 * 결론:
 * Viewport는 사용자가 웹페이지를 보는 브라우저 화면 영역을 의미하며,
 * 모바일 기기와 같은 다양한 해상도에서 웹페이지의 표시 방식을 제어하는 데 매우 중요한 역할을 합니다.
 * 이를 적절히 설정하면 반응형 웹 디자인을 구현할 수 있습니다.
 */
